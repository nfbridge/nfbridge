# Neo Film Bridge v0.7.2 preview r11 — F100 플래시 촬영정보 해석 교정

이번 릴리스는 새 기능이 아니라, 기존 F100 촬영정보 중 플래시 관련 필드의 해석(디코딩) 정확성을 실물 근거로 교정한 최소 유지보수입니다. 카메라 통신·연결·설정·삭제 절차는 전혀 바뀌지 않았습니다.

[English](RELEASE_NOTES.en.md)

## 무엇이 달라졌는가

플래시 관련 두 필드의 해석이 다음과 같이 교정됐습니다.

**플래시 종류(flash type)**
- 이전: `off`/`ttl` 두 값만 인식하고, 그 외 값은 비트 마스크 자체가 구조적으로 어긋나 있어 조건에 따라 정상 값도 `unknown`으로 잘못 새는 경우가 있었습니다.
- 교정: 해당 필드는 2비트 값이며, `off`(꺼짐)·`non_ttl`(Non-TTL)·`ttl`(TTL) 세 값을 정확한 비트 범위에서 판정합니다. 관측되지 않은 나머지 한 값은 계속 `unknown`으로 남습니다.

**플래시 발광 시점(flash sync)**
- 이전: `Normal`·`Rear`(후막) 두 값만 인식했습니다.
- 교정: `Slow`(슬로우 동조)·`Red-eye`(적목감소)·`Red-eye + Slow`(적목감소+슬로우) 세 값이 추가됐습니다. 실제 카메라가 생성하는지 확인되지 않은 나머지 값들은 계속 `unknown`으로 남으며, 추측으로 라벨을 붙이지 않았습니다.

플래시 보정(EV)과 다중노출 플래그는 기존 판정 로직이 실물 값과 이미 정확히 일치함을 재확인했을 뿐, 코드 변경은 없습니다.

## 실물 근거

이번 교정은 실제 F100으로 촬영한 두 롤의 촬영정보를 직접 읽어, 각 컷의 플래시 상태(꺼짐/Non-TTL/TTL, 발광 시점 5종, 보정값, 다중노출)를 독립적인 별도 판독 결과와 컷 단위로 전수 대조한 뒤에만 반영했습니다. 추측이나 재구성 없이, 실제 관측된 값만 정본으로 삼았습니다.

## 과거 문서 오류 정정 안내

이번 실물 재확인 과정에서, 이 프로젝트의 과거 초기 분석 기록 중 일부에 플래시 종류(TTL/Non-TTL)와 발광 시점(Slow/Rear) 라벨이 서로 뒤바뀌어 적힌 부분이 있었음이 드러났습니다. 비트 위치 자체는 처음부터 맞았고, 값에 붙인 의미(라벨)만 틀려 있었습니다. 해당 과거 기록은 소급 수정하지 않고 그대로 두었으며, 현재 코드와 문서에만 교정된 라벨을 반영했습니다.

## 영향 범위

- 카메라와의 통신 방식, USB/serial 연결 판정, 설정 변경·삭제 절차, 앱 구조, 패키징: 전혀 변경 없음
- 출력 JSON의 필드 이름과 구조(schema), 값의 출처 표기(provenance) 정책: 전혀 변경 없음 — 기존에 이 파일을 읽고 있는 외부 도구가 있어도 영향 없습니다.

## Offline 회귀

- `tests/`: 168 PASS
- `mac-connection/`: 57 PASS
- `mac-client/`: 115 PASS, 1 SKIP(연구용 케이블 식별 도구가 공개 소스 패키지에서 제외되어 있다는 기존·무관 skip)
- 저장소 전체 Python 컴파일 검사: 통과
- golden 비교(`empty_roll`/`simple_one_roll`/`detailed_two_rolls`): 3/3 통과
- `MANIFEST.sha256`: 114/114 통과

## 지원 범위

실행 파일 대상은 Apple Silicon이며 macOS 14.0 이상을 declares합니다. 앱은 ad-hoc 서명됐고 Apple 공증은 받지 않았습니다 — 이 상태는 이번 변경으로 바뀌지 않았습니다.
