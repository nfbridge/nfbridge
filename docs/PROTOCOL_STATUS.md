# Protocol status

CQ/MQ/OQ/LQ are read-only. MQ/OQ/LQ were observed on the physical Mac setup described in VALIDATION.md. An empty 79 permits one retry; its official Nikon name is unknown. The 16-bit frame length counts status plus payload. Unknown record bits remain raw; external value tables are not bundled.
