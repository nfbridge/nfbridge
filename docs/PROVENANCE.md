# Provenance boundary

The code was developed for interoperability research using public technical
material, legacy reference applications, and independently created tests. Those
outside inputs remain outside this export.

This public source repository carries only:

- project-authored source and documentation;
- project-authored synthetic fixtures designed not to reproduce source files;
- names and high-level factual descriptions needed to explain evidence roles;
- an optional `pyserial==3.5` requirement with its published package hash.

This document is not a complete legal clearance. The public source export received
human review before publication; Neo Film Bridge's own code is GPLv3-only.


Named dependencies and research inputs are recorded in [ACKNOWLEDGEMENTS.md](ACKNOWLEDGEMENTS.md).
