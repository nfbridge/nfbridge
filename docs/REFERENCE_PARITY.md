# Reference parity

The normative schema and detailed comparison contract are distributed beside
the client in `mac-client/REFERENCE_PARITY.md`.

Bundled references are synthetic and can establish only deterministic offline
decoder behavior. A future application-derived reference requires separately
approved hardware observation, immutable private evidence, and a public-safe
derived record. `MATCH` never authenticates an application, cable, or camera.
