# Offline reproducibility

Run ./verify.sh from a clean checkout. It verifies file hashes, compiles Python, runs synthetic tests and three reference comparisons. No hardware access or private research directory is needed. Real 117-frame replay is excluded; see VALIDATION.md.
